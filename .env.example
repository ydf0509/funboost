# Funboost Web Manager 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ==================== 服务器配置 ====================

# 监听地址（默认: 0.0.0.0）
FUNBOOST_WEB_HOST=0.0.0.0

# 监听端口（默认: 27018）
FUNBOOST_WEB_PORT=27018

# 是否开启调试模式（默认: false）
# 生产环境请设置为 false
FUNBOOST_DEBUG=false

# ==================== 安全配置 ====================

# Flask 密钥（生产环境必须修改！）
# 建议使用随机生成的长字符串
FUNBOOST_SECRET_KEY=your-secret-key-change-in-production

# ==================== Session 配置 ====================

# Session Cookie 是否仅通过 HTTPS 传输（默认: false）
# 生产环境使用 HTTPS 时设置为 true
FUNBOOST_SESSION_SECURE=false

# Session Cookie SameSite 策略（默认: Lax）
# 可选值: Strict, Lax, None
# - Strict: 最严格，跨站请求不发送 Cookie
# - Lax: 默认值，导航到目标网站的 GET 请求会发送 Cookie
# - None: 跨站请求也发送 Cookie（需要 Secure=true）
FUNBOOST_SESSION_SAMESITE=Lax

# ==================== CORS 配置 ====================

# 允许的跨域来源（逗号分隔）
# 同源部署时留空即可
# 示例: http://localhost:3000,http://127.0.0.1:3000
FUNBOOST_CORS_ORIGINS=

# ==================== 前端配置 ====================

# 是否启用前端服务（默认: true）
# 如果只需要 API 服务，可设置为 false
FUNBOOST_FRONTEND_ENABLED=true

# 前端 URL（用于邮件中的链接）
# 同源部署时可留空，系统会自动检测
FRONTEND_URL=
